# Лабораторна робота 3
## Модульне тестування програмного коду (Unit Testing)

**Дисципліна:** Основи програмної інженерії  
**Мова програмування:** C++ (стандарт C++17)  
**Фреймворк тестування:** Google Test (GTest) 1.14.0  
**Інструмент покриття:** lcov / gcov  
**Github: https://github.com/SomeoneHere-git/SomeoneHere-git.github.io**

---

## 1. Мета роботи

Набуття практичних навичок написання модульних тестів з використанням Google Test.  
Оволодіння техніками EP (Equivalence Partitioning) та BVA (Boundary Value Analysis).  
Досягнення line coverage ≥ 80 % для реалізованих модулів.

---

## 2. Вихідний код реалізованого модуля

Реалізовано два класи на основі UML-моделі з ЛР 02 (система управління командами):

### `src/task.h` / `src/task.cpp` — клас Task

```cpp
enum class TaskStatus { TODO, IN_PROGRESS, DONE };

class Task {
public:
    std::string taskId, title, assigneeId;
    TaskStatus  status;

    Task(const std::string& taskId,
         const std::string& title,
         const std::string& assigneeId = "");

    void        updateProgress(TaskStatus newStatus); // допустимі переходи: TODO→IN_PROGRESS→DONE
    std::string getAssignee()  const;                 // "(unassigned)" якщо пусто
    bool        isCompleted()  const;                 // true якщо DONE
};
```

Ключова логіка `updateProgress`:
- `TODO → IN_PROGRESS` — дозволено
- `IN_PROGRESS → DONE` — дозволено
- Всі інші переходи → `std::logic_error`

### `src/team.h` / `src/team.cpp` — клас Team

```cpp
class Team {
public:
    std::string teamId, name, leaderId;

    Team(const std::string& teamId,
         const std::string& name,
         const std::string& leaderId);

    void                     join(const std::string& userId); // кидає виняток при дублікаті
    std::vector<std::string> getMembers()      const;
    size_t                   getMemberCount()  const;
    bool                     hasMember(const std::string& userId) const;
};
```

---

## 3. Таблиця проєктування тестів

| № | Тест-кейс | Метод | Вхідні дані | Очікуваний результат | Техніка | Статус |
|---|-----------|-------|-------------|----------------------|---------|--------|
| 1 | ValidArguments_EP_Positive | `Task()` | id="t1", title="Fix bug", assignee="user42" | Об'єкт створено, status=TODO | EP+ | ✅ pass |
| 2 | EmptyTaskId_EP_Negative | `Task()` | id="", title="Fix bug" | `invalid_argument` | EP− | ✅ pass |
| 3 | EmptyTitle_EP_Negative | `Task()` | id="t1", title="" | `invalid_argument` | EP− | ✅ pass |
| 4 | NoAssignee_EP_Positive | `Task()` | id="t2", title="Write docs" | assigneeId порожній, status=TODO | EP+ | ✅ pass |
| 5 | TodoToInProgress_EP_Positive | `updateProgress()` | TODO → IN_PROGRESS | status=IN_PROGRESS | EP+ | ✅ pass |
| 6 | InProgressToDone_EP_Positive | `updateProgress()` | IN_PROGRESS → DONE | status=DONE | EP+ | ✅ pass |
| 7 | TodoToDone_EP_Negative | `updateProgress()` | TODO → DONE | `logic_error` | EP− | ✅ pass |
| 8 | DoneToInProgress_EP_Negative | `updateProgress()` | DONE → IN_PROGRESS | `logic_error` | EP− | ✅ pass |
| 9 | TodoToTodo_BVA_Negative | `updateProgress()` | TODO → TODO (межа: той самий стан) | `logic_error` | BVA | ✅ pass |
| 10 | WithAssignee_EP_Positive | `getAssignee()` | assigneeId="user99" | повертає "user99" | EP+ | ✅ pass |
| 11 | WithoutAssignee_EP_Positive | `getAssignee()` | assigneeId="" | повертає "(unassigned)" | EP+ | ✅ pass |
| 12 | StatusTodo_BVA | `isCompleted()` | status=TODO (нижня межа) | false | BVA | ✅ pass |
| 13 | StatusInProgress_BVA | `isCompleted()` | status=IN_PROGRESS (проміжна межа) | false | BVA | ✅ pass |
| 14 | StatusDone_BVA | `isCompleted()` | status=DONE (верхня межа) | true | BVA | ✅ pass |
| 15 | ValidArguments_EP_Positive | `Team()` | name="Alpha", leaderId="leader1" | Об'єкт, memberCount=0 | EP+ | ✅ pass |
| 16 | EmptyName_EP_Negative | `Team()` | name="" | `invalid_argument` | EP− | ✅ pass |
| 17 | EmptyLeaderId_EP_Negative | `Team()` | leaderId="" | `invalid_argument` | EP− | ✅ pass |
| 18 | AddFirstMember_BVA | `join()` | userId="user1" (межа 0→1) | memberCount=1, hasMember=true | BVA | ✅ pass |
| 19 | AddMultipleMembers_EP_Positive | `join()` | user1, user2, user3 | memberCount=3 | EP+ | ✅ pass |
| 20 | DuplicateMember_EP_Negative | `join()` | user1 двічі | `runtime_error` | EP− | ✅ pass |
| 21 | EmptyUserId_EP_Negative | `join()` | userId="" | `invalid_argument` | EP− | ✅ pass |
| 22 | EmptyTeam_BVA | `hasMember()` | порожня команда (нижня межа) | false | BVA | ✅ pass |
| 23 | MemberExists_EP_Positive | `hasMember()` | userId є у команді | true | EP+ | ✅ pass |
| 24 | MemberNotExists_EP_Positive | `hasMember()` | userId відсутній | false | EP+ | ✅ pass |
| 25 | ReturnsCorrectList_EP_Positive | `getMembers()` | userA, userB | вектор {userA, userB} | EP+ | ✅ pass |

**Загальна кількість тест-кейсів: 25**  
**EP+ (позитивні):** 12 | **EP− (негативні):** 8 | **BVA:** 5

---

---

## 4. Вихідний код тестового набору

Нижче наведено код модульних тестів (файл `tests/test_main.cpp`). Кожен тест розроблено за патерном AAA (Arrange - Act - Assert) із застосуванням технік EP та BVA:

<details>
<summary>Натисніть, щоб розгорнути код тестів</summary>

```cpp
#include <gtest/gtest.h>
#include "../src/task.h"
#include "../src/team.h"

// ============================================================
//  ТЕСТИ КЛАСУ Task
// ============================================================

// ---- Конструктор: EP (допустимі / недопустимі аргументи) ----

TEST(TaskConstructor, ValidArguments_EP_Positive) {
    // Arrange
    std::string id = "t1", title = "Fix bug", assignee = "user42";

    // Act
    Task task(id, title, assignee);

    // Assert
    EXPECT_EQ(task.taskId,     "t1");
    EXPECT_EQ(task.title,      "Fix bug");
    EXPECT_EQ(task.assigneeId, "user42");
    EXPECT_EQ(task.status,     TaskStatus::TODO);
    // Техніка: EP — допустимий клас (всі поля непорожні)
}

TEST(TaskConstructor, EmptyTaskId_EP_Negative) {
    // Arrange + Act + Assert
    // Техніка: EP — недопустимий клас (порожній taskId)
    EXPECT_THROW(Task("", "Fix bug"), std::invalid_argument);
}

TEST(TaskConstructor, EmptyTitle_EP_Negative) {
    // Техніка: EP — недопустимий клас (порожній title)
    EXPECT_THROW(Task("t1", ""), std::invalid_argument);
}

TEST(TaskConstructor, NoAssignee_EP_Positive) {
    // Техніка: EP — допустимий клас (assigneeId не обов'язковий)
    Task task("t2", "Write docs");
    EXPECT_TRUE(task.assigneeId.empty());
    EXPECT_EQ(task.status, TaskStatus::TODO);
}

// ---- updateProgress: EP (допустимі переходи) + BVA (межі стану) ----

TEST(TaskUpdateProgress, TodoToInProgress_EP_Positive) {
    // Arrange
    Task task("t1", "Do work");

    // Act
    task.updateProgress(TaskStatus::IN_PROGRESS);

    // Assert
    EXPECT_EQ(task.status, TaskStatus::IN_PROGRESS);
    // Техніка: EP — допустимий перехід TODO→IN_PROGRESS
}

TEST(TaskUpdateProgress, InProgressToDone_EP_Positive) {
    // Arrange
    Task task("t1", "Do work");
    task.updateProgress(TaskStatus::IN_PROGRESS);

    // Act
    task.updateProgress(TaskStatus::DONE);

    // Assert
    EXPECT_EQ(task.status, TaskStatus::DONE);
    // Техніка: EP — допустимий перехід IN_PROGRESS→DONE
}

TEST(TaskUpdateProgress, TodoToDone_EP_Negative) {
    // Техніка: EP — недопустимий перехід TODO→DONE (пропуск стану)
    Task task("t1", "Do work");
    EXPECT_THROW(task.updateProgress(TaskStatus::DONE), std::logic_error);
}

TEST(TaskUpdateProgress, DoneToInProgress_EP_Negative) {
    // Техніка: EP — недопустимий перехід (задача вже закрита)
    Task task("t1", "Do work");
    task.updateProgress(TaskStatus::IN_PROGRESS);
    task.updateProgress(TaskStatus::DONE);
    EXPECT_THROW(task.updateProgress(TaskStatus::IN_PROGRESS), std::logic_error);
}

TEST(TaskUpdateProgress, TodoToTodo_BVA_Negative) {
    // Техніка: BVA — повторний перехід у той самий стан (межа: однаковий статус)
    Task task("t1", "Do work");
    EXPECT_THROW(task.updateProgress(TaskStatus::TODO), std::logic_error);
}

// ---- getAssignee: EP (порожній / непорожній assigneeId) ----

TEST(TaskGetAssignee, WithAssignee_EP_Positive) {
    // Техніка: EP — assigneeId непорожній
    Task task("t1", "Task", "user99");
    EXPECT_EQ(task.getAssignee(), "user99");
}

TEST(TaskGetAssignee, WithoutAssignee_EP_Positive) {
    // Техніка: EP — assigneeId порожній → повертає "(unassigned)"
    Task task("t1", "Task");
    EXPECT_EQ(task.getAssignee(), "(unassigned)");
}

// ---- isCompleted: BVA (межі статусу) ----

TEST(TaskIsCompleted, StatusTodo_BVA) {
    // Техніка: BVA — стан TODO (нижня межа)
    Task task("t1", "Task");
    EXPECT_FALSE(task.isCompleted());
}

TEST(TaskIsCompleted, StatusInProgress_BVA) {
    // Техніка: BVA — стан IN_PROGRESS (проміжна межа)
    Task task("t1", "Task");
    task.updateProgress(TaskStatus::IN_PROGRESS);
    EXPECT_FALSE(task.isCompleted());
}

TEST(TaskIsCompleted, StatusDone_BVA) {
    // Техніка: BVA — стан DONE (верхня межа)
    Task task("t1", "Task");
    task.updateProgress(TaskStatus::IN_PROGRESS);
    task.updateProgress(TaskStatus::DONE);
    EXPECT_TRUE(task.isCompleted());
}

// ============================================================
//  ТЕСТИ КЛАСУ Team
// ============================================================

// ---- Конструктор: EP ----

TEST(TeamConstructor, ValidArguments_EP_Positive) {
    // Техніка: EP — допустимий клас
    Team team("team1", "Alpha", "leader1");
    EXPECT_EQ(team.name,     "Alpha");
    EXPECT_EQ(team.leaderId, "leader1");
    EXPECT_EQ(team.getMemberCount(), 0u);
}

TEST(TeamConstructor, EmptyName_EP_Negative) {
    // Техніка: EP — недопустимий клас (порожня назва)
    EXPECT_THROW(Team("team1", "", "leader1"), std::invalid_argument);
}

TEST(TeamConstructor, EmptyLeaderId_EP_Negative) {
    // Техніка: EP — недопустимий клас (порожній leaderId)
    EXPECT_THROW(Team("team1", "Alpha", ""), std::invalid_argument);
}

// ---- join: EP + BVA (межа: 0 учасників / 1 / дублікат) ----

TEST(TeamJoin, AddFirstMember_BVA) {
    // Техніка: BVA — перший учасник (межа: розмір 0→1)
    Team team("team1", "Alpha", "leader1");
    team.join("user1");
    EXPECT_EQ(team.getMemberCount(), 1u);
    EXPECT_TRUE(team.hasMember("user1"));
}

TEST(TeamJoin, AddMultipleMembers_EP_Positive) {
    // Техніка: EP — допустимий клас (кілька різних userId)
    Team team("team1", "Alpha", "leader1");
    team.join("user1");
    team.join("user2");
    team.join("user3");
    EXPECT_EQ(team.getMemberCount(), 3u);
}

TEST(TeamJoin, DuplicateMember_EP_Negative) {
    // Техніка: EP — недопустимий клас (дублікат userId)
    Team team("team1", "Alpha", "leader1");
    team.join("user1");
    EXPECT_THROW(team.join("user1"), std::runtime_error);
}

TEST(TeamJoin, EmptyUserId_EP_Negative) {
    // Техніка: EP — недопустимий клас (порожній userId)
    Team team("team1", "Alpha", "leader1");
    EXPECT_THROW(team.join(""), std::invalid_argument);
}

// ---- hasMember: BVA (порожня команда / є учасник / нема) ----

TEST(TeamHasMember, EmptyTeam_BVA) {
    // Техніка: BVA — порожня команда (нижня межа)
    Team team("team1", "Alpha", "leader1");
    EXPECT_FALSE(team.hasMember("user1"));
}

TEST(TeamHasMember, MemberExists_EP_Positive) {
    // Техніка: EP — учасник є у команді
    Team team("team1", "Alpha", "leader1");
    team.join("user1");
    EXPECT_TRUE(team.hasMember("user1"));
}

TEST(TeamHasMember, MemberNotExists_EP_Positive) {
    // Техніка: EP — учасника немає у команді
    Team team("team1", "Alpha", "leader1");
    team.join("user1");
    EXPECT_FALSE(team.hasMember("user999"));
}

// ---- getMembers: повертає правильний список ----

TEST(TeamGetMembers, ReturnsCorrectList_EP_Positive) {
    // Техніка: EP — позитивний сценарій
    Team team("team1", "Alpha", "leader1");
    team.join("userA");
    team.join("userB");
    auto members = team.getMembers();
    ASSERT_EQ(members.size(), 2u);
    EXPECT_EQ(members[0], "userA");
    EXPECT_EQ(members[1], "userB");
}
```

---

## 5. Звіт покриття коду

```
Summary coverage rate:
  lines......: 100.0% (48 of 48 lines)
  functions..: 100.0% (9 of 9 functions)
```

| Файл | Рядків | Покрито | Coverage |
|------|--------|---------|----------|
| src/task.cpp | 25 | 25 | **100 %** |
| src/team.cpp | 23 | 23 | **100 %** |
| **TOTAL** | **48** | **48** | **100 %** |

> Порогове значення ≥ 80 % — **виконано** ✅

---

## 6. Інструкція для запуску

```bash
# Клонувати репозиторій та перейти у папку lab3
sudo apt install cmake g++ lcov libgtest-dev   # Ubuntu/Debian

# Збірка з підтримкою покриття
cmake -S . -B build -DENABLE_COVERAGE=ON
cmake --build build -j4

# Запуск тестів
./build/lab3_tests

# Генерація HTML-звіту покриття
cd build
lcov --capture --directory . --output-file cov.info --ignore-errors inconsistent
lcov --remove cov.info '*/googletest/*' '/usr/*' -o cov_filtered.info --ignore-errors inconsistent
genhtml cov_filtered.info --output-directory coverage_html --ignore-errors inconsistent
# Відкрити: coverage_html/index.html
```

---

## 7. Структура проєкту

```
lab3/
├── CMakeLists.txt
├── src/
│   ├── task.h
│   ├── task.cpp
│   ├── team.h
│   └── team.cpp
└── tests/
    └── test_main.cpp
```

---

## 8. Висновки

У ході виконання лабораторної роботи реалізовано два класи (`Task`, `Team`) на основі UML-діаграми з ЛР 02 та написано 25 модульних тестів з використанням Google Test.

**Результати покриття:** line coverage — 100 %, function coverage — 100 %, що значно перевищує мінімальний поріг 80 %.

Застосування технік EP та BVA дозволило систематично охопити всі класи еквівалентності: допустимі аргументи, порожні рядки, дублікати, недопустимі переходи між станами, граничні значення (порожня колекція, перший елемент, крайні стани enum). Завдяки цьому жодних непокритих шляхів виконання не залишилося.

Виявлена проблема: branch coverage не вимірюється lcov без додаткових прапорців компілятора (`-fprofile-arcs -ftest-coverage`). У реальному проєкті рекомендується також відстежувати branch coverage ≥ 70 %.
